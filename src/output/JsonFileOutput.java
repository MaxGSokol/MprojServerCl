package output;

public class JsonFileOutput implements FileOutputType {
    @Override
    public void outputData(String data) {
   /*     try (FileWriter fileWriter = new FileWriter(ServerClConfig.JSON_FILE_OUTPUT)) {

        } catch (IOException e) {
            throw new RuntimeException(e);
        }*/
    }
}
